# Reference architecture

Sensors feed a microcontroller or gateway, which validates units and timestamps, stores readings, and sends bounded measurements to a dashboard. Separate composter and hydroponics control logic, include calibration records, and fail safely when a sensor is unavailable.
